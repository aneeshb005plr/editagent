# EditEdge - requirements.txt
# Every version below was checked against the real package index
# (pip index versions) or installed and introspected in a sandbox -
# not filled in from memory. Re-verify before each dependency bump,
# same discipline, don't assume this list stays current.

# --- Web framework ---
fastapi==0.128.1
# 0.128.1, not 0.128.0 - 0.128.0 has a confirmed issue with PEP 649
# deferred-annotation resolution on newer Python; harmless on 3.13
# but costs nothing to pin the fixed version now.
uvicorn[standard]==0.34.0

# --- Settings / validation ---
pydantic==2.10.4
pydantic-settings==2.7.0

# --- MongoDB ---
pymongo==4.10.1
langgraph-checkpoint-mongodb==0.4.0
# Confirmed via sandbox introspection: this version exposes ONLY the
# sync MongoDBSaver (pymongo.synchronous.MongoClient) - its aput/aget
# etc. are real coroutine functions that internally wrap the sync
# client. No dedicated async-native saver class exists in this
# release, despite some external docs implying otherwise - verified
# by actually installing and inspecting the package, not trusted
# from search results alone.

# --- LangGraph / orchestration ---
langgraph==1.2.10
langchain-core==1.5.3
langchain-openai==1.4.2
# Only needed at the chunking boundary (converting ParsedDocument
# blocks into langchain_core.documents.Document for the text
# splitter) - deliberately NOT a dependency of app/documents/*,
# which stays framework-agnostic. See docx/pptx parser docstrings.

# --- Auth ---
pyjwt==2.10.1
cryptography==44.0.0

# --- Document parsing ---
python-docx==1.2.0
python-pptx==1.0.2
openpyxl==3.1.5
pymupdf==1.28.2
pymupdf4llm==1.28.2

# --- Microsoft 365 Agents SDK (added when channel integration starts) ---
# microsoft-agents-hosting-core==<pin when we reach that phase>

# --- Dev / testing ---
pytest==8.3.4
pytest-asyncio==0.25.0